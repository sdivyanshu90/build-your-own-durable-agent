Add a configurable retry limit to the sample service, update its documentation, preserve
backward compatibility with the historical three-attempt default, validate positive
limits, and prove the change using automated tests.
